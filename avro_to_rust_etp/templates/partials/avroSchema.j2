{#- {%- macro get_json_schema(schema) -%}
	#[inline]
	pub fn avro_schema() -> String {
		String::from(r#"{{ json.dumps(schema) }}"#)
	} 
{%- endmacro -%}
{%- block schema -%} -#}
{#- 	{{get_json_schema(file.expanded_schema)}} -#}
{#- {%- endblock -%} -#}
r#"{{json.dumps(file.expanded_schema, indent=4)}}"#
{#- r#"{{json.dumps(file.schema, indent=4)}}"# -#}
{#-  -#}