{%- block union -%}
    {%- if field.optional -%}
    {% set ns.optional = True %}
    {%- if field.union_types|length > 1 -%}
    Option<{{get_union_types_enum_name(get_union_types(field, True, None))}}>
    {%- else -%}
    Option<{{get_union_types_enum_name(get_union_types(field, True, None))}}>
    {%- endif -%} 
    {%- else -%}
    {{get_union_types_enum_name(get_union_types(field, True, None))}}
    {%- endif -%} 
{%- endblock -%}